<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" <%= link_to  "Home",  root_path %> <span class="sr-only">(current)</span></a>
      </li>
      <% if user_signed_in? %>

      <li class="nav-item">
        <a class="nav-link" <%= link_to "Create Event", new_event_path %> <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item dropdown">
        <div class="dropdown-menu" aria-labelledby="dropdown01">
          <a class="dropdown-item" <%= link_to "My profile", user_path(current_user) %></a>
          <a class="dropdown-item" <%= link_to "Log out", destroy_user_session_path, method: :delete %></a>
        </div>
      </li>
        <li class="nav-item">
          <a class="nav-link" <%= link_to "Edit profile", edit_user_registration_path %> <span class="sr-only">(current)</span></a>
        </li>
        <% else %>
          <div class="dropdown">
          <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Sign up / Sign in
          </button>
              <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                <a class="dropdown-item" <%= link_to "Sign Up", new_user_registration_path %></a>
                <a class="dropdown-item" <%= link_to "Sign In", new_user_session_path %></a>
              </div>
        </div>

  <% end %>

    </ul>

  </div>
</nav>
